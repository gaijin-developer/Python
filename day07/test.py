# names = ["john smith", "jay santi", "eva kuki"]
#
# names = [name.title() for name in names]
#
# print(names)
# usernames = ["john 1990", "alberta1970", "magnola2000"]
#
# names = [len(name) for name in usernames]
#
# print(names)
user_entries = ['10', '19.1', '20']
sum = 0
for entry in user_entries:
    entry = float(entry)
    sum = sum + entry
print(sum)
